/**
 * sort field for product.
 */
export enum ProductSortField {
  Name = 'name',
  Price = 'price',
  CreatedAt = 'createdAt',
}

/**
 * the sort order for the application.
 */
export enum SortOrder {
  Asc = 'asc',
  Desc = 'desc',
}
